function openVault(){

    //Write the String with the phrase provided
    let userMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination: ";

    //create variables using any mathematical operation to assign the correct value to each variable 

        //value must be 10
        let a = 2 * 5;

        //value must be 40
        let b = 120 / 3;

        //value must be 39
        let c = 7 * 6 - 3;

        //Send out alert on page with message and combination by concatenating the variables
        alert(userMessage + a + "-" + b + "-" + c);
}

openVault();

